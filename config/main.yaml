defaults:
  - process: process
  - model: model
  - datasets:
    - turbofan
    - sampledata2
  - _self_

raw:
  dir: data/raw/

processed:
  dir: data/processed
  name: processed.csv
  path: ${processed.dir}/${processed.name}

final:
  dir: data/final
  name: final.csv
  path: ${final.dir}/${final.name}
defaults:
  - process: turbofan
  - model: model
  - _self_

raw:
  dir: data/raw/
  name: train_FD001.txt
  path: ${raw.dir}/${raw.name}

processed:
  dir: data/processed/
  input_name: input_processed.csv
  input_path: ${processed.dir}/${processed.input_name}
  output_name: output_processed.csv
  output_path: ${processed.dir}/${processed.output_name}

final:
  dir: data/final/
  name: final.csv
  path: ${final.dir}/${final.name}
Module src.process
==================

Functions
---------

    
`process_data(config)`
:   Function to process the data
    Requires configuration file

    
`run(config: omegaconf.dictconfig.DictConfig)`
: